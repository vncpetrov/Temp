@{
    this.ViewData["Title"] = "Buses";
}

<div class="text-center mt-5 mb-3 h1">@this.ViewData["Title"]</div>

<div class="text-center mt-5 mb-3 h4">@this.ViewData["Title"] with this destinations do not exist yet.</div>

<div class="d-flex justify-content-center"><a class="btn btn-primary" asp-controller="Buses" asp-action="CheckDestinationsId">Try other destinations</a></div>